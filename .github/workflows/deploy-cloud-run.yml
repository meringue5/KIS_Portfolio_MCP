name: Deploy Cloud Run

on:
  workflow_dispatch:
    inputs:
      target:
        description: Deploy target
        required: true
        default: all
        type: choice
        options:
          - all
          - auth
          - remote
          - batch
          - scheduler
          - overseas-batch
          - overseas-scheduler
          - token-warmup-batch
          - token-warmup-scheduler

permissions:
  contents: read
  id-token: write

concurrency:
  group: deploy-cloud-run-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    env:
      GOOGLE_CLOUD_PROJECT: ${{ vars.GOOGLE_CLOUD_PROJECT }}
      KIS_DEPLOY_REGION: ${{ vars.KIS_DEPLOY_REGION }}
      KIS_AUTH_SERVICE_NAME: ${{ vars.KIS_AUTH_SERVICE_NAME }}
      KIS_REMOTE_SERVICE_NAME: ${{ vars.KIS_REMOTE_SERVICE_NAME }}
      KIS_BATCH_JOB_NAME: ${{ vars.KIS_BATCH_JOB_NAME }}
      KIS_BATCH_SCHEDULER_NAME: ${{ vars.KIS_BATCH_SCHEDULER_NAME }}
      KIS_OVERSEAS_BATCH_JOB_NAME: ${{ vars.KIS_OVERSEAS_BATCH_JOB_NAME }}
      KIS_OVERSEAS_BATCH_SCHEDULER_NAME: ${{ vars.KIS_OVERSEAS_BATCH_SCHEDULER_NAME }}
      KIS_TOKEN_WARMUP_JOB_NAME: ${{ vars.KIS_TOKEN_WARMUP_JOB_NAME }}
      KIS_TOKEN_WARMUP_SCHEDULER_NAME: ${{ vars.KIS_TOKEN_WARMUP_SCHEDULER_NAME }}
      KIS_CLOUD_SCHEDULER_REGION: ${{ vars.KIS_CLOUD_SCHEDULER_REGION }}
      GOOGLE_CLOUD_PROJECT_NUMBER: ${{ vars.GOOGLE_CLOUD_PROJECT_NUMBER }}
      KIS_CLOUD_SCHEDULER_INVOKER_SERVICE_ACCOUNT: ${{ vars.KIS_CLOUD_SCHEDULER_INVOKER_SERVICE_ACCOUNT }}
      KIS_DEPLOY_ENV: ${{ secrets.KIS_DEPLOY_ENV }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          python-version: "3.13"
          enable-cache: true

      - name: Validate workflow configuration
        run: |
          test -n "${GOOGLE_CLOUD_PROJECT}" || { echo "GOOGLE_CLOUD_PROJECT var is required."; exit 1; }
          test -n "${KIS_DEPLOY_ENV}" || { echo "KIS_DEPLOY_ENV secret is required."; exit 1; }

      - name: Write deployment env file
        run: |
          uv run python - <<'PY'
          import os
          from pathlib import Path

          env_text = os.environ.get("KIS_DEPLOY_ENV", "")
          if not env_text.strip():
              raise SystemExit("KIS_DEPLOY_ENV secret is empty.")
          Path(".env").write_text(env_text, encoding="utf-8")
          PY

      - name: Run test suite
        run: uv run pytest

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
          project_id: ${{ vars.GOOGLE_CLOUD_PROJECT }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v3
        with:
          project_id: ${{ vars.GOOGLE_CLOUD_PROJECT }}

      - name: Deploy auth service
        if: github.event.inputs.target == 'all' || github.event.inputs.target == 'auth'
        run: |
          uv run python scripts/deploy_cloud_run.py auth \
            --project "${GOOGLE_CLOUD_PROJECT}" \
            --region "${KIS_DEPLOY_REGION:-asia-northeast3}" \
            --service "${KIS_AUTH_SERVICE_NAME:-kis-portfolio-auth}"

      - name: Deploy remote service
        if: github.event.inputs.target == 'all' || github.event.inputs.target == 'remote'
        run: |
          uv run python scripts/deploy_cloud_run.py remote \
            --project "${GOOGLE_CLOUD_PROJECT}" \
            --region "${KIS_DEPLOY_REGION:-asia-northeast3}" \
            --service "${KIS_REMOTE_SERVICE_NAME:-kis-portfolio-remote}"

      - name: Deploy domestic order history batch job
        if: github.event.inputs.target == 'all' || github.event.inputs.target == 'batch'
        run: |
          uv run python scripts/deploy_cloud_run.py batch \
            --project "${GOOGLE_CLOUD_PROJECT}" \
            --region "${KIS_DEPLOY_REGION:-asia-northeast3}" \
            --job "${KIS_BATCH_JOB_NAME:-kis-portfolio-domestic-order-history}"

      - name: Deploy domestic order history scheduler
        if: github.event.inputs.target == 'all' || github.event.inputs.target == 'scheduler'
        run: |
          uv run python scripts/deploy_cloud_run.py scheduler \
            --project "${GOOGLE_CLOUD_PROJECT}" \
            --region "${KIS_DEPLOY_REGION:-asia-northeast3}" \
            --job "${KIS_BATCH_JOB_NAME:-kis-portfolio-domestic-order-history}" \
            --scheduler "${KIS_BATCH_SCHEDULER_NAME:-kis-portfolio-domestic-order-history-1535}" \
            --scheduler-region "${KIS_CLOUD_SCHEDULER_REGION:-${KIS_DEPLOY_REGION:-asia-northeast3}}"

      - name: Deploy overseas transaction history batch job
        if: github.event.inputs.target == 'all' || github.event.inputs.target == 'overseas-batch'
        run: |
          uv run python scripts/deploy_cloud_run.py overseas-batch \
            --project "${GOOGLE_CLOUD_PROJECT}" \
            --region "${KIS_DEPLOY_REGION:-asia-northeast3}" \
            --job "${KIS_OVERSEAS_BATCH_JOB_NAME:-kis-portfolio-overseas-transaction-history}"

      - name: Deploy overseas transaction history scheduler
        if: github.event.inputs.target == 'all' || github.event.inputs.target == 'overseas-scheduler'
        run: |
          uv run python scripts/deploy_cloud_run.py overseas-scheduler \
            --project "${GOOGLE_CLOUD_PROJECT}" \
            --region "${KIS_DEPLOY_REGION:-asia-northeast3}" \
            --job "${KIS_OVERSEAS_BATCH_JOB_NAME:-kis-portfolio-overseas-transaction-history}" \
            --scheduler "${KIS_OVERSEAS_BATCH_SCHEDULER_NAME:-kis-portfolio-overseas-transaction-history-0735}" \
            --scheduler-region "${KIS_CLOUD_SCHEDULER_REGION:-${KIS_DEPLOY_REGION:-asia-northeast3}}"

      - name: Deploy token warm-up dry-run batch job
        if: github.event.inputs.target == 'all' || github.event.inputs.target == 'token-warmup-batch'
        run: |
          uv run python scripts/deploy_cloud_run.py token-warmup-batch \
            --project "${GOOGLE_CLOUD_PROJECT}" \
            --region "${KIS_DEPLOY_REGION:-asia-northeast3}" \
            --job "${KIS_TOKEN_WARMUP_JOB_NAME:-kis-portfolio-token-warmup-dry-run}"

      - name: Deploy token warm-up dry-run scheduler
        if: github.event.inputs.target == 'all' || github.event.inputs.target == 'token-warmup-scheduler'
        run: |
          uv run python scripts/deploy_cloud_run.py token-warmup-scheduler \
            --project "${GOOGLE_CLOUD_PROJECT}" \
            --region "${KIS_DEPLOY_REGION:-asia-northeast3}" \
            --job "${KIS_TOKEN_WARMUP_JOB_NAME:-kis-portfolio-token-warmup-dry-run}" \
            --scheduler "${KIS_TOKEN_WARMUP_SCHEDULER_NAME:-kis-portfolio-token-warmup-0830}" \
            --scheduler-region "${KIS_CLOUD_SCHEDULER_REGION:-${KIS_DEPLOY_REGION:-asia-northeast3}}"
